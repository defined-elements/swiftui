import Foundation

/// [DE] Global preset protocol.
///
/// Used for modules to support global preset setting function.
public protocol DefinedPresetProtocol {
    // No need for anything.
    // We are just going to cast things when handling them.
}
